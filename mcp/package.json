{
  "name": "askpass-sudo-ssh",
  "version": "1.0.0",
  "description": "MCP server that runs one sudo command on a remote host, asking for the password locally through askpass.",
  "type": "module",
  "bin": {
    "askpass-sudo-ssh": "index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.2",
    "zod": "^3.25.76"
  },
  "os": [
    "linux"
  ],
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SilentAutomaton/askpass.git"
  }
}
